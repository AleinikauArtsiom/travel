server:
    port: 8080

  spring:
    datasource:
      url: jdbc:postgresql://localhost:5432/travel_pet_db
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
    flyway:
      enable: true
      locations: classpath:db/migration

    jpa:
      hibernate:
        ddl-auto: update
